client mikrotik {
    ipaddr      = YOUR_MIKROTIK_IP
    secret      = your-strong-shared-secret
    shortname   = mikrotik
    nas_type    = other
}

# Allow the Docker host itself for testing
client localhost {
    ipaddr = 127.0.0.1
    secret = your-strong-shared-secret
}